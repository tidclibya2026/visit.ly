import { CheckCircle2, FileImage, Landmark, Layers3, LogIn, Plus, RefreshCw, Send, ShieldCheck, Sparkles, TicketCheck } from "lucide-react";
import { ChangeEvent, FormEvent, useEffect, useState } from "react";
import { Link } from "wouter";
import { SiteShell } from "@/components/SiteShell";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";

type Status = "draft" | "published" | "archived";
const blankDestination = { slug: "", title: "", city: "", region: "", category: "heritage" as const, description: "", imageUrl: "", status: "draft" as Status };
const blankExperience = { slug: "", title: "", destinationSlug: "", region: "", season: "", description: "", imageUrl: "", status: "draft" as Status };
const blankSection = { slug: "", sectionType: "festival" as const, title: "", summary: "", imageUrl: "", status: "draft" as Status };

const Field = ({ label, value, onChange, multiline = false, type = "text" }: { label: string; value: string; onChange: (value: string) => void; multiline?: boolean; type?: string }) => <label className="admin-field"><span>{label}</span>{multiline ? <textarea value={value} onChange={(event) => onChange(event.target.value)} /> : <input type={type} value={value} onChange={(event) => onChange(event.target.value)} />}</label>;
const StatusSelect = ({ value, onChange }: { value: Status; onChange: (value: Status) => void }) => <label className="admin-field"><span>حالة النشر</span><select value={value} onChange={(event) => onChange(event.target.value as Status)}><option value="draft">مسودة</option><option value="published">منشور</option><option value="archived">مؤرشف</option></select></label>;

export default function ContentManagement() {
  const { user, loading, isAuthenticated } = useAuth();
  const content = trpc.contentAdmin.list.useQuery(undefined, { enabled: user?.role === "admin" });
  const visa = trpc.visa.listIntakes.useQuery(undefined, { enabled: user?.role === "admin" });
  const utils = trpc.useUtils();
  const refresh = () => { utils.contentAdmin.list.invalidate(); utils.visa.listIntakes.invalidate(); };
  const createDestination = trpc.contentAdmin.createDestination.useMutation({ onSuccess: refresh });
  const createExperience = trpc.contentAdmin.createExperience.useMutation({ onSuccess: refresh });
  const updateExperience = trpc.contentAdmin.updateExperience.useMutation({ onSuccess: refresh });
  const createSection = trpc.contentAdmin.createSection.useMutation({ onSuccess: refresh });
  const uploadImage = trpc.contentAdmin.uploadImage.useMutation({ onSuccess: refresh });
  const updateVisa = trpc.visa.updateIntakeStatus.useMutation({ onSuccess: refresh });
  const [destination, setDestination] = useState(blankDestination);
  const [experience, setExperience] = useState(blankExperience);
  const [experienceId, setExperienceId] = useState<number | null>(null);
  const [section, setSection] = useState(blankSection);
  const [media, setMedia] = useState({ altText: "", sourceLabel: "مركز المعلومات والتوثيق السياحي", caption: "", file: null as File | null });
  const [notice, setNotice] = useState("");
  useEffect(() => { if (experienceId) { const item = content.data?.experiences.find((row) => row.id === experienceId); if (item) setExperience({ slug: item.slug, title: item.title, destinationSlug: item.destinationSlug ?? "", region: item.region, season: item.season ?? "", description: item.description, imageUrl: item.imageUrl ?? "", status: item.status }); } }, [experienceId, content.data?.experiences]);
  if (loading) return <SiteShell><section className="page-frame admin-review-state"><RefreshCw className="spin" /><p>جارٍ التحقق من صلاحية الإدارة…</p></section></SiteShell>;
  if (!isAuthenticated) return <SiteShell><section className="page-frame admin-review-state"><ShieldCheck size={34} /><h1>إدارة المحتوى</h1><p>سجل الدخول بصلاحية مشرف لإضافة وتحرير المحتوى والوسائط ومراجعة طلبات التأشيرة.</p><button className="button button-ink" type="button" onClick={startLogin}><LogIn size={16} /> تسجيل الدخول</button></section></SiteShell>;
  if (user?.role !== "admin") return <SiteShell><section className="page-frame admin-review-state"><ShieldCheck size={34} /><h1>وصول مقيّد</h1><p>تحتاج إلى صلاحية مشرف لإدارة محتوى Visit Libya.</p><Link href="/" className="button button-ink">العودة للمنصة</Link></section></SiteShell>;
  const submitDestination = (event: FormEvent) => { event.preventDefault(); createDestination.mutate({ ...destination, imageUrl: destination.imageUrl || null }, { onSuccess: () => { setDestination(blankDestination); setNotice("تم حفظ الوجهة في قاعدة البيانات."); } }); };
  const submitExperience = (event: FormEvent) => { event.preventDefault(); const value = { ...experience, destinationSlug: experience.destinationSlug || null, season: experience.season || null, imageUrl: experience.imageUrl || null }; const done = () => { setExperience(blankExperience); setExperienceId(null); setNotice("تم حفظ التجربة في قاعدة البيانات."); }; if (experienceId) updateExperience.mutate({ id: experienceId, value }, { onSuccess: done }); else createExperience.mutate(value, { onSuccess: done }); };
  const submitSection = (event: FormEvent) => { event.preventDefault(); createSection.mutate({ ...section, imageUrl: section.imageUrl || null }, { onSuccess: () => { setSection(blankSection); setNotice("تمت إضافة القسم في قاعدة البيانات."); } }); };
  const upload = (event: FormEvent) => { event.preventDefault(); if (!media.file) return; const reader = new FileReader(); reader.onload = () => uploadImage.mutate({ fileName: media.file!.name, mimeType: media.file!.type as "image/jpeg" | "image/png" | "image/webp", dataBase64: String(reader.result), altText: media.altText, sourceLabel: media.sourceLabel, caption: media.caption || null }, { onSuccess: () => { setMedia({ altText: "", sourceLabel: "مركز المعلومات والتوثيق السياحي", caption: "", file: null }); setNotice("تم رفع الصورة وتسجيل بياناتها الموثقة."); } }); reader.readAsDataURL(media.file); };
  return <SiteShell><section className="page-frame content-admin-head"><div><p className="eyebrow"><ShieldCheck size={14} /> إدارة المنصة</p><h1>محتوى حيّ، تحت مراجعة واضحة.</h1><p>أضف مسودات أو محتوى منشورًا إلى قاعدة البيانات. لا تُنشئ هذه اللوحة صورًا أو بيانات تجريبية، ويظهر كل أصل مع مصدره.</p></div><span><Sparkles size={16} /> مشرف: {user.name || "معتمد"}</span></section>{notice && <p className="page-frame admin-notice"><CheckCircle2 size={16} /> {notice}</p>}<section className="page-frame content-admin-grid"><form onSubmit={submitDestination} className="content-admin-form"><p className="eyebrow"><Landmark size={14} /> وجهة جديدة</p><h2>إضافة وجهة</h2><Field label="المعرف اللاتيني" value={destination.slug} onChange={(value) => setDestination({ ...destination, slug: value })} /><Field label="اسم الوجهة" value={destination.title} onChange={(value) => setDestination({ ...destination, title: value })} /><div className="admin-pair"><Field label="المدينة" value={destination.city} onChange={(value) => setDestination({ ...destination, city: value })} /><Field label="المنطقة" value={destination.region} onChange={(value) => setDestination({ ...destination, region: value })} /></div><label className="admin-field"><span>الفئة</span><select value={destination.category} onChange={(event) => setDestination({ ...destination, category: event.target.value as typeof destination.category })}><option value="heritage">تراث</option><option value="nature">طبيعة</option><option value="coast">ساحل</option><option value="city">مدينة</option></select></label><Field label="الوصف" value={destination.description} onChange={(value) => setDestination({ ...destination, description: value })} multiline /><Field label="رابط الصورة (اختياري)" value={destination.imageUrl} onChange={(value) => setDestination({ ...destination, imageUrl: value })} /><StatusSelect value={destination.status} onChange={(value) => setDestination({ ...destination, status: value })} /><button className="button button-ink" disabled={createDestination.isPending}><Plus size={16} /> حفظ الوجهة</button></form><form onSubmit={submitExperience} className="content-admin-form"><p className="eyebrow"><Sparkles size={14} /> تجربة</p><h2>{experienceId ? "تعديل تجربة" : "إضافة تجربة"}</h2><label className="admin-field"><span>اختر تجربة لتعديلها (اختياري)</span><select value={experienceId ?? ""} onChange={(event) => setExperienceId(event.target.value ? Number(event.target.value) : null)}><option value="">تجربة جديدة</option>{content.data?.experiences.map((item) => <option key={item.id} value={item.id}>{item.title}</option>)}</select></label><Field label="المعرف اللاتيني" value={experience.slug} onChange={(value) => setExperience({ ...experience, slug: value })} /><Field label="اسم التجربة" value={experience.title} onChange={(value) => setExperience({ ...experience, title: value })} /><div className="admin-pair"><Field label="معرف الوجهة المرتبطة" value={experience.destinationSlug} onChange={(value) => setExperience({ ...experience, destinationSlug: value })} /><Field label="الموسم" value={experience.season} onChange={(value) => setExperience({ ...experience, season: value })} /></div><Field label="المنطقة" value={experience.region} onChange={(value) => setExperience({ ...experience, region: value })} /><Field label="الوصف" value={experience.description} onChange={(value) => setExperience({ ...experience, description: value })} multiline /><Field label="رابط الصورة (اختياري)" value={experience.imageUrl} onChange={(value) => setExperience({ ...experience, imageUrl: value })} /><StatusSelect value={experience.status} onChange={(value) => setExperience({ ...experience, status: value })} /><button className="button button-ink" disabled={createExperience.isPending || updateExperience.isPending}><CheckCircle2 size={16} /> حفظ التجربة</button></form><form onSubmit={submitSection} className="content-admin-form"><p className="eyebrow"><Layers3 size={14} /> قسم محتوى</p><h2>أضف مهرجانًا أو قسمًا</h2><Field label="المعرف اللاتيني" value={section.slug} onChange={(value) => setSection({ ...section, slug: value })} /><label className="admin-field"><span>نوع القسم</span><select value={section.sectionType} onChange={(event) => setSection({ ...section, sectionType: event.target.value as typeof section.sectionType })}><option value="festival">مهرجان</option><option value="culture">ثقافة</option><option value="heritage">تراث</option><option value="travel">سفر</option><option value="custom">قسم خاص</option></select></label><Field label="العنوان" value={section.title} onChange={(value) => setSection({ ...section, title: value })} /><Field label="الملخص" value={section.summary} onChange={(value) => setSection({ ...section, summary: value })} multiline /><Field label="رابط الصورة (اختياري)" value={section.imageUrl} onChange={(value) => setSection({ ...section, imageUrl: value })} /><StatusSelect value={section.status} onChange={(value) => setSection({ ...section, status: value })} /><button className="button button-ink" disabled={createSection.isPending}><Plus size={16} /> حفظ القسم</button></form><form onSubmit={upload} className="content-admin-form"><p className="eyebrow"><FileImage size={14} /> أرشيف الوسائط</p><h2>إضافة صورة موثقة</h2><label className="admin-field"><span>الصورة</span><input type="file" accept="image/jpeg,image/png,image/webp" onChange={(event: ChangeEvent<HTMLInputElement>) => setMedia({ ...media, file: event.target.files?.[0] ?? null })} /></label><Field label="النص البديل" value={media.altText} onChange={(value) => setMedia({ ...media, altText: value })} /><Field label="مصدر الصورة" value={media.sourceLabel} onChange={(value) => setMedia({ ...media, sourceLabel: value })} /><Field label="تعليق توثيقي (اختياري)" value={media.caption} onChange={(value) => setMedia({ ...media, caption: value })} multiline /><button className="button button-ink" disabled={!media.file || uploadImage.isPending}><FileImage size={16} /> رفع وحفظ البيانات</button></form></section><section className="page-frame visa-admin-queue"><p className="eyebrow"><TicketCheck size={14} /> مسار التأشيرة</p><h2>طلبات محفوظة بانتظار الإحالة الرسمية</h2><p>لا ترسل المنصة أي طلب إلى جهة خارجية تلقائيًا. غيّر الحالة إلى «جاهز للإحالة» فقط بعد اعتماد القناة الرسمية.</p>{visa.data?.length ? <div>{visa.data.map((item) => <article key={item.id}><div><strong>{item.referenceCode}</strong><span>{item.fullName} · {item.nationality}</span><small>{item.email}</small></div><label><span>الحالة</span><select value={item.status} onChange={(event) => updateVisa.mutate({ id: item.id, status: event.target.value as "received" | "ready_for_official_referral" | "closed" })}><option value="received">تم الاستلام</option><option value="ready_for_official_referral">جاهز للإحالة الرسمية</option><option value="closed">مغلق</option></select></label></article>)}</div> : <p>لا توجد طلبات مسجلة حتى الآن.</p>}</section></SiteShell>;
}
